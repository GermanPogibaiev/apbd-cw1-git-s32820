Fast-forward happens when main has no new commits after branch creation. A merge commit is created when both branches have new commits.
Merge keeps branch history. Rebase rewrites commits on top of a new base and makes history linear.
The conflict was resolved manually by editing the conflicting code, keeping the final correct version, and committing the fix.